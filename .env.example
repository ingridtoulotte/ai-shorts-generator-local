# Optional cloud upgrades. Leave empty to run fully local.
# Groq LLM for higher-quality scripts (https://console.groq.com)
GROQ_API_KEY=
# PlayAI for cloud TTS (needed on Linux / Render where Windows SAPI is unavailable)
PLAYAI_API_KEY=
# Server port (Render sets this automatically)
PORT=3000
